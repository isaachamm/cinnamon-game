# 0.0.2
- Resolved bug where board doesn't get reset on date change by adding `resetBoard` function to currDate effect
- Added random date button so that you can play on a random day between the current date and the earliest date available.